# 流程自动化工具 v1.6 - 使用说明

## 快速开始

双击 **启动工具.bat** 即可运行。

## v1.6 更新了什么

**修复了自绘界面（微信、浏览器、Electron 应用）捕获必然失败的缺陷。**

用真实窗口做了完整诊断（记事本=原生控件、微信=自绘界面，14 项测试全过），找到的根因：

> 鼠标悬停在微信这类自绘软件上时，Windows 控件接口返回的就是"整个窗口"，
> 而旧代码找所属窗口时只向上找父级、从不检查"控件自己是不是窗口"——
> 结果窗口标题为空、兜底坐标也没记录，**捕获出来的步骤回放必然报错**。

v1.6 的修复：
1. **窗口查找三重保障** — 先查自身 → 再查父级链 → 最后按进程 ID 兜底，任何情况下都能定位所属窗口
2. **捕获信息升级** — 始终记录：窗口标题 + 窗口类名 + 窗口内相对位置 + 屏幕绝对坐标，四层保障
3. **窗口类名兜底** — 回放时标题匹配失败自动改用类名匹配（标题会变，类名不会），浏览器换了页面流程照样跑
4. **DPI 感知** — 高分屏缩放（125%/150%）下坐标不再错位

## v1.5 更新了什么

- 智能点击带回退：捕获时记录按钮在窗口内的相对位置，回放时先按控件名找，找不到就按位置点
- 坐标点击自动绑定窗口：窗口挪了位置，点击依然准确
- 单步测试：选中任何一步，点「▶ 测试选中」单独运行

## 界面说明

| 按钮 | 功能 |
|------|------|
| 🎯 捕获点击 | 智能捕获：窗口标题+类名+相对位置+绝对坐标，四层保障 |
| 📍 坐标点击 | 记录鼠标位置，自动绑定所在窗口 |
| ▶ 测试选中 | 只运行选中的那一步（调试神器） |
| ⏱ 等待 | 添加等待步骤（等页面加载等） |
| ⌨ 输入文字 | 在当前位置输入一段文字（支持中文，通过剪贴板粘贴） |
| 🔗 快捷键 | 添加快捷键操作（如 ctrl+c、ctrl+v、enter） |
| 🪟 切换窗口 | 切换到指定窗口（会列出所有打开的窗口供选择） |
| 🌐 打开网址 | 在默认浏览器中打开一个网址 |

## 使用流程

### 1. 录制你的打卡流程

1. 先手动操作一遍，记住每一步
2. 在工具里按顺序添加步骤
3. 对于需要点击的按钮，用 **🎯 捕获点击** 来录制
4. 每录一步，点 **▶ 测试选中** 验证一下能不能点中

### 2. 示例：DeepSeek → 微信打卡

已附带示例流程 `flows/example_deepseek_wechat.json`，用 **打开** 按钮加载即可：

```
1. 切换到 DeepSeek 窗口
2. 等待 1 秒
3. Ctrl+A（全选文章内容）
4. Ctrl+C（复制）
5. 切换到微信窗口
6. 等待 1 秒
7. Ctrl+V（粘贴）
8. Enter（发送）
```

> 注意：Ctrl+A 会选中整个页面。如果只想复制回答内容，
> 可以先用「捕获点击」点中回答区域，再 Ctrl+A。

### 3. 保存和复用

- 点 **保存** 把流程存成 `.json` 文件
- 下次点 **打开** 加载，点 **▶ 运行** 就自动执行
- 旧版 v1.0 的流程文件可以直接加载，完全兼容

## 打包成 exe

双击 **build_exe.bat**，打包完成后 exe 在 `dist/` 目录下。

## 注意事项

- 微信窗口标题必须包含"微信"两个字才能被找到（标题失配时会自动用窗口类名兜底）
- 智能点击的兜底位置是"窗口内相对坐标"，窗口挪动不影响；但如果软件内部界面变了（换了皮肤、改了版式），需要重新捕获
- 捕获时如果提示"自绘界面，回放按位置点击"，是正常现象（微信/浏览器都这样），位置兜底会生效
- **如果目标软件以管理员身份运行，本工具必须也以管理员身份运行**，否则 Windows 安全机制会拦截点击
- 网页/应用加载慢时，在步骤间插入「等待」步骤，避免点太快
- 步骤之间会自动加 0.3 秒间隔，防止操作太快
- 运行中可以随时点 **■ 停止**

## 自检

怀疑识别功能有问题时，运行 `python diagnose.py`，它会在真实窗口（记事本+微信）上跑完整测试并输出每项 PASS/FAIL。
