# 流程自动化工具 - 使用说明

## 快速开始

双击 **启动工具.bat** 即可运行。

## 界面说明

| 按钮 | 功能 |
|------|------|
| 🎯 捕获点击 | 智能捕获控件（按名称/ID识别，窗口挪了也能找到） |
| 📍 坐标点击 | 纯坐标点击（只记位置，简单直接，适合捕获不到的软件） |
| ⏱ 等待 | 添加等待步骤（等页面加载等） |
| ⌨ 输入文字 | 在当前位置输入一段文字（支持中文，通过剪贴板粘贴） |
| 🔗 快捷键 | 添加快捷键操作（如 ctrl+c、ctrl+v、enter） |
| 🪟 切换窗口 | 切换到指定窗口（会列出所有打开的窗口供选择） |
| 🌐 打开网址 | 在默认浏览器中打开一个网址 |

## 使用流程

### 1. 录制你的打卡流程

1. 先手动操作一遍，记住每一步
2. 在工具里按顺序添加步骤
3. 对于需要点击的按钮，用 **🎯 捕获点击** 来录制

### 2. 示例：DeepSeek → 微信打卡

已附带示例流程 `flows/example_deepseek_wechat.json`，用 **打开** 按钮加载即可：

```
1. 切换到 DeepSeek 窗口
2. 等待 1 秒
3. Ctrl+A（全选文章内容）
4. Ctrl+C（复制）
5. 切换到微信窗口
6. 等待 1 秒
7. Ctrl+V（粘贴）
8. Enter（发送）
```

> 注意：Ctrl+A 会选中整个页面。如果只想复制回答内容，
> 可以先用「捕获点击」点中回答区域，再 Ctrl+A。

### 3. 保存和复用

- 点 **保存** 把流程存成 `.json` 文件
- 下次点 **打开** 加载，点 **▶ 运行** 就自动执行

## 打包成 exe

双击 **build_exe.bat**，打包完成后 exe 在 `dist/` 目录下。

## 注意事项

- 微信窗口标题必须包含"微信"两个字才能被找到
- 如果某个按钮捕获不到（有些软件用的是自定义绘制），可以改用 **📍 坐标点击** 或快捷键代替
- **坐标点击** 只记录鼠标位置，如果窗口挪了位置或分辨率变了，需要重新录制
- 步骤之间会自动加 0.3 秒间隔，防止操作太快
- 运行中可以随时点 **■ 停止**
