# 流程自动化工具 v1.5 - 使用说明

## 快速开始

双击 **启动工具.bat** 即可运行。

## v1.5 更新了什么

**核心改进：点击可靠性。**

之前的版本捕获微信/浏览器里的按钮经常失败，因为这类软件是自绘界面，Windows 控件识别看不到它们的名字。v1.5 的解决方案：

1. **智能点击带回退** — 捕获时同时记录按钮在窗口内的相对位置。回放时先按控件名找，找不到就直接按位置点。就算控件识别是瞎的，位置兜底照样点得中。
2. **坐标点击绑定窗口** — 捕获坐标时自动识别鼠标所在的窗口，记录的是窗口内相对位置。窗口挪到哪里都点得准。
3. **单步测试** — 选中任何一步，点「▶ 测试选中」单独运行，不用跑整个流程来调试。

## 界面说明

| 按钮 | 功能 |
|------|------|
| 🎯 捕获点击 | 智能捕获：记录控件信息 + 窗口内位置（双保险） |
| 📍 坐标点击 | 记录鼠标位置，自动绑定所在窗口 |
| ▶ 测试选中 | 只运行选中的那一步（调试神器） |
| ⏱ 等待 | 添加等待步骤（等页面加载等） |
| ⌨ 输入文字 | 在当前位置输入一段文字（支持中文，通过剪贴板粘贴） |
| 🔗 快捷键 | 添加快捷键操作（如 ctrl+c、ctrl+v、enter） |
| 🪟 切换窗口 | 切换到指定窗口（会列出所有打开的窗口供选择） |
| 🌐 打开网址 | 在默认浏览器中打开一个网址 |

## 使用流程

### 1. 录制你的打卡流程

1. 先手动操作一遍，记住每一步
2. 在工具里按顺序添加步骤
3. 对于需要点击的按钮，用 **🎯 捕获点击** 来录制
4. 每录一步，点 **▶ 测试选中** 验证一下能不能点中

### 2. 示例：DeepSeek → 微信打卡

已附带示例流程 `flows/example_deepseek_wechat.json`，用 **打开** 按钮加载即可：

```
1. 切换到 DeepSeek 窗口
2. 等待 1 秒
3. Ctrl+A（全选文章内容）
4. Ctrl+C（复制）
5. 切换到微信窗口
6. 等待 1 秒
7. Ctrl+V（粘贴）
8. Enter（发送）
```

> 注意：Ctrl+A 会选中整个页面。如果只想复制回答内容，
> 可以先用「捕获点击」点中回答区域，再 Ctrl+A。

### 3. 保存和复用

- 点 **保存** 把流程存成 `.json` 文件
- 下次点 **打开** 加载，点 **▶ 运行** 就自动执行
- 旧版 v1.0 的流程文件可以直接加载，完全兼容

## 打包成 exe

双击 **build_exe.bat**，打包完成后 exe 在 `dist/` 目录下。

## 注意事项

- 微信窗口标题必须包含"微信"两个字才能被找到
- 智能点击的兜底位置是"窗口内相对坐标"，窗口挪动、改布局不影响；但如果软件内部界面变了（比如换了皮肤、改了版式），需要重新捕获
- 捕获时如果提示"控件无名，将按位置点击"，是正常现象（微信/浏览器都这样），不影响使用
- 步骤之间会自动加 0.3 秒间隔，防止操作太快
- 运行中可以随时点 **■ 停止**
